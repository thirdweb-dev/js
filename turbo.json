{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "outputs": ["dist/**"],
      "dependsOn": ["^build"]
    },
    "sdk#build": {
      "dependsOn": ["^build"]
    },
    "auth#build": {
      "outputs": ["dist/**", "express/**", "next/**", "next-auth/**"]
    },
    "solana#build": {
      "outputs": ["dist/**", "server/**"]
    },
    "test": {
      "outputs": [],
      "inputs": ["src/**", "test/**"],
      "dependsOn": ["build"]
    },
    "lint": {
      "outputs": []
    },
    "dev": {
      "cache": false
    },
    "clean": {
      "cache": false
    }
  }
}
