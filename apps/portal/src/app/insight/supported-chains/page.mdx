import { createMetadata } from "@doc";

export const metadata = createMetadata({
  title: "thirdweb Insight - Chains compatibles",
  description: "Lista de cadenas indexadas por thirdweb Insight.",
  image: {
    title: "thirdweb Insight - Chains compatibles",
    icon: "insight",
  },
});

# Cadenas compatibles

[Ver todas las cadenas indexadas a través de thirdweb Insight](https://thirdweb.com/chainlist?service=insight).

Si deseas sugerir una cadena para que la apoyemos, [contáctanos](https://thirdweb.com/contact-us).
