import { Callout } from "@doc";
import { createMetadata, DocImage } from "@doc";

export const metadata = createMetadata({
	title: "Getting started with Chainsaw",
	description:
		"Learn how to use Chainsaw to access onchain data",
});

# Get Started

// TODO: Add content