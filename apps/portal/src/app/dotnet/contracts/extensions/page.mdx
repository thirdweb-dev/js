import { Details, createMetadata } from "@doc";

export const metadata = createMetadata({
	title: "Extensiones de Contrato | Thirdweb .NET SDK",
	description: "Extensiones útiles y seguras por tipo para llamar a contratos.",
});

# Extensiones de Contrato

El SDK de .NET de Thirdweb proporciona un conjunto de extensiones útiles y seguras por tipo para llamar a contratos inteligentes. Estas extensiones simplifican el proceso de interacción con los contratos inteligentes al proporcionar una API más intuitiva y fácil de usar.

## Uso


```csharp
// Escribir sin extensiones
var receiver = await wallet.GetAddress();
var quantity = BigInteger.One;
var currency = Constants.NATIVE_TOKEN_ADDRESS;
var pricePerToken = BigInteger.Zero;
var allowlistProof = new object[] { new byte[] { }, BigInteger.Zero, BigInteger.Zero, Constants.ADDRESS_ZERO };
var data = new byte[] { };
var receipt = await contract.Write(smartAccount, contract, "claim", 0, receiver, quantity, currency, pricePerToken, allowlistProof, data);

// Escribir con extensiones
var receipt = await contract.DropERC20_Claim(wallet, receiver, amount);

// Leer sin extensiones
var balance = await contract.Read<string>("balanceOf", "0xOwnerAddress");

// Leer con extensiones
var balance = await contract.ERC20_BalanceOf("0xOwnerAddress");

// Generar calldata de bajo nivel
var calldata = contract.CreateCallData("myFunction", param1, param2);
```

## Extensiones de Contrato Disponibles

Consulta la [referencia completa](https://thirdweb-dev.github.io/dotnet/docs/Thirdweb.ThirdwebExtensions.html) de `ThirdwebExtensions` para obtener una lista completa de las extensiones de contrato disponibles.

Si estás utilizando nuestro contrato de [Marketplace](https://thirdweb.com/thirdweb.eth/MarketplaceV3), echa un vistazo a nuestras [extensiones específicas para Marketplace](https://thirdweb-dev.github.io/dotnet/docs/Thirdweb.ThirdwebMarketplaceExtensions.html).


