import { OpenSourceCard, DocImage, createMetadata } from "@doc";

export const metadata = createMetadata({
	title: "Thirdweb .NET SDK in Unity",
	description:
		"A guide on how to integrate the Thirdweb .NET SDK into your Unity (tested with Unity 2022.3+) projects to add blockchain functionality.",
});

# Integrating into Unity projects

Coming soon!