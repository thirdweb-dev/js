import { Blueprint } from "../blueprint";
import {
	createWalletNode,
	disconnectNode,
	fetchOAuthLoginLinkNode,
	isConnectedNode,
	linkWalletNode,
	sendOtpNode,
	signInNode,
} from "./nodes";
import { createUnrealEngineMetadata } from "../../util";

export const metadata = createUnrealEngineMetadata({
	title: "Billeteras In-App",
	description: "Blueprints de Billeteras In-App",
});

# Billeteras In-App

### Crear Billetera
Crea una billetera in-app basada en los inputs proporcionados.
<Blueprint code={createWalletNode} className='w-full h-48' />

### Iniciar Sesión
Inicia sesión, pasando el input requerido si es OTP, JWT o Auth Endpoint
<Blueprint code={signInNode} className='w-full h-40' />

### Vincular Billetera
Vincula un nuevo método de autenticación con una billetera creada - pero que nunca se ha iniciado sesión
<Blueprint code={linkWalletNode} className='w-full h-40' />

## OTP

### Enviar OTP
Envía un OTP para la billetera in-app.
<Blueprint code={sendOtpNode} className='w-full h-40' />

## OAuth

### Obtener enlace de login OAuth
Obtiene el enlace de login OAuth para la billetera in-app.
<Blueprint code={fetchOAuthLoginLinkNode} className='w-full h-40' />

## Utilidades

### ¿Está Conectado?
Verifica si el manejador de billetera está conectado a una sesión. Variantes booleanas y macro
<Blueprint code={isConnectedNode} className='w-full h-48' />

### Desconectar
Desconecta el manejador de billetera de su sesión.
<Blueprint code={disconnectNode} className='w-full h-28' />
