import { createUnrealEngineMetadata } from "../../util";
import {
	equalityNode,
	isChecksummedAddressNode,
	isValidAddressNode,
	isValidNode,
	oAuthProviderToStringText,
	signMessageNode,
	stringTextToOAuthProvider,
	toChecksummedAddressNode,
	toTextStringNode,
	zeroAddressNode,
} from "./nodes";
import { Blueprint } from "../blueprint";

export const metadata = createUnrealEngineMetadata({
	title: "Utilidades",
	description: "Funciones de utilidad y ayuda",
});

# Utilidades

### Firmar Mensaje
Firma un mensaje con el manejador de billetera especificado.
<Blueprint code={signMessageNode} className='w-full h-48' />

### Convertir a Texto/Cadena
Convierte un mensaje a texto/cadena con el manejador de billetera especificado.
<Blueprint code={toTextStringNode} className='w-full h-40' />

### Dirección Válida
Verifica la validez de la dirección.
<Blueprint code={isValidAddressNode} className='w-full h-32' />

### Dirección con Suma de Control
Verifica si una dirección tiene suma de control válida.
<Blueprint code={isChecksummedAddressNode} className='w-full h-32' />

### Convertir a Dirección con Suma de Control
Convierte una dirección a su versión con suma de control.
<Blueprint code={toChecksummedAddressNode} className='w-full h-32' />

### Es Válido
Verifica si un valor es válido.
<Blueprint code={isValidNode} className='w-full h-48' />

### Igualdad
Operadores nativos de igualdad/desigualdad.
<Blueprint code={equalityNode} className='w-full h-60' />

### Dirección Cero
Dirección constante cero.
<Blueprint code={zeroAddressNode} className='w-full h-28' />

### OAuth Provider a Texto/Cadena
Convierte un EThirdwebOAuthProvider a texto/cadena.
<Blueprint code={oAuthProviderToStringText} className="w-full h-32" />

### Texto/Cadena a OAuth Provider
Convierte texto/cadena a un EThirdwebOAuthProvider.
<Blueprint code={stringTextToOAuthProvider} className="w-full h-32" />
