import { Blueprint } from "../blueprint";
import { getTransactionReceiptNode, getTransactionStatusNode, readContractNode, writeContractNode } from "./nodes";
import { createUnrealEngineMetadata } from "../../util";

export const metadata = createUnrealEngineMetadata({ title: "Engine", description: "Blueprints de Engine" });

# Engine

## Interacción con Contratos

### Leer Contrato
Llama al endpoint de Leer Contrato en una instancia de engine.
<Blueprint code={readContractNode} className='w-full h-48' />

### Escribir Contrato
Llama al endpoint de Escribir Contrato en una instancia de engine.
<Blueprint code={writeContractNode} className='w-full h-64' />

### Obtener Estado de la Transacción
Obtén el estado de la transacción de un ID de cola en una instancia de engine.
<Blueprint code={getTransactionStatusNode} className='w-full h-48' />

### Obtener Recibo de la Transacción
Obtén el recibo de la transacción de un hash de transacción desde una cadena.
<Blueprint code={getTransactionReceiptNode} className='w-full h-48' />
