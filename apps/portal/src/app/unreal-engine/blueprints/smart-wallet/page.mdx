import { Blueprint } from "../blueprint";
import { createUnrealEngineMetadata } from "../../util";
import {
	addAdminNode,
	createSessionKeyNode,
	createSmartWalletNode,
	getActiveSignersNode,
	getAdminsNode,
	isActiveSignerNode,
	isDeployedNode,
	removeAdminNode,
	revokeSessionKeyNode,
} from "./nodes";

export const metadata = createUnrealEngineMetadata({
	title: "Billeteras Inteligentes",
	description: "Blueprints de Billeteras Inteligentes",
});

# Billeteras Inteligentes

### Crear Billetera Inteligente
Crea una billetera in-app por correo electrónico.
<Blueprint code={createSmartWalletNode} className='w-full h-60' />

### ¿Está Desplegada?
Verifica si la billetera inteligente está desplegada.
<Blueprint code={isDeployedNode} className='w-full h-48' />

### Crear Clave de Sesión
Crea una clave de sesión para la billetera inteligente.
<Blueprint code={createSessionKeyNode} className='w-full h-60' />

### Obtener Administradores
Recupera la lista de administradores de la billetera inteligente.
<Blueprint code={getAdminsNode} className='w-full h-48' />

### Obtener Firmantes Activos
Recupera la lista de firmantes activos de la billetera inteligente.
<Blueprint code={getActiveSignersNode} className='w-full h-48' />

### ¿Es Firmante Activo?
Verifica si un firmante específico es válido.
<Blueprint code={isActiveSignerNode} className='w-full h-48' />

### Revocar Clave de Sesión
Revoca una clave de sesión de un firmante de billetera inteligente.
<Blueprint code={revokeSessionKeyNode} className='w-full h-48' />

### Agregar Administrador
Agrega un firmante administrador a la billetera inteligente.
<Blueprint code={addAdminNode} className='w-full h-48' />

### Eliminar Administrador
Elimina un firmante administrador de la billetera inteligente.
<Blueprint code={removeAdminNode} className='w-full h-48' />
