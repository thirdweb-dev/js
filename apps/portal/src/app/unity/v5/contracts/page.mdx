import { Details, Callout, createMetadata, ArticleIconCard } from "@doc";
import { GraduationCap } from "lucide-react";

export const metadata = createMetadata({
	title: "ThirdwebContract | SDK de thirdweb para Unity",
	description:
		"Instancia un ThirdwebContract para firmar transacciones y mensajes.",
});

# ThirdwebContract

`ThirdwebContract` es un envoltorio alrededor de cualquier contrato inteligente compatible con EVM. Te permite interactuar fácilmente con las funciones del contrato, así como preparar transacciones.

### Instanciando un ThirdwebContract

```csharp
var contract = await ThirdwebManager.Instance.GetContract(
	address: "contract-address", 
	chainId: 1, 
	abi: "optional-abi"
);
```

¡Eso es todo! Ahora puedes interactuar con el contrato usando `ThirdwebContract`.

Contiene métodos `Read`, `Write` y `Prepare` para respectivamente leer el estado del contrato, escribir en el contrato y preparar transacciones.

Además, contiene atajos como `ThirdwebContract.ERC20_BalanceOf` para interactuar fácilmente con interfaces comunes de contratos.

<ArticleIconCard
	href="/dotnet/contracts/read"
	icon={GraduationCap}
	title="Interactuar con Contratos Inteligentes"
/>
