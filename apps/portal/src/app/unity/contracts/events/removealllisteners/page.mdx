import { createMetadata } from "@doc";

export const metadata = createMetadata({
	title: "contract.Events.RemoveAllListeners | thirdweb Unity SDK",
	description:
		"References for contract.Events.RemoveAllListeners in thirdweb Unity SDK.",
});

# RemoveAllListeners

Remove all previously created event listeners, such as those created with [ListenToAll](unity/events/listentoall).

## Usage

```csharp
await contract.Events.RemoveAllListeners();
```
