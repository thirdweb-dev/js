import { createMetadata } from "@doc";

export const metadata = createMetadata({
	title: "wallet.Sign | thirdweb Unity SDK",
	description: "Sign any message with the connected wallet",
});

# Sign

Sign any message with the connected wallet. (personal_sign)

## Usage

Create a message to be signed and pass it to this method.

Once signed, the signature is returned.

Uses EIP-1271 to sign the message when using compatible Smart Wallets.

```csharp
var data = await sdk.Wallet.Sign("my message");
```

## Configuration

### message

The message to be signed.

Must be a `string`.

## Return Value

Returns a `string` with the signature.

```csharp
string
```
