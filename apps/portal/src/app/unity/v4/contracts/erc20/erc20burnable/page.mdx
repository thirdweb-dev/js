import { Details, createMetadata } from "@doc";

export const metadata = createMetadata({
	title: "ERC20Burnable | thirdweb Unity SDK",
	description: "References for ERC20Burnable contract in thirdweb Unity SDK.",
});

# ERC20Burnable

Allows you to burn tokens (take them out of circulation).

## Burn

Burn tokens held by the **connected wallet**.

```csharp
var data = await contract.ERC20.Burn("{{amount}}");
```

<Details summary="Configuration">

#### amount

The amount of this token you want to burn.

Must be a `string`.

</Details>
