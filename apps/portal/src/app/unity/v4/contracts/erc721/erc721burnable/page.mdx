import { Details, createMetadata } from "@doc";

export const metadata = createMetadata({
	title: "ERC721Burnable | thirdweb Unity SDK",
	description: "References for ERC721Burnable contract in thirdweb Unity SDK.",
});

# ERC721Burnable

Allows you to burn an NFT (transfer it to a **null address** that is unrecoverable).

## Burn

Burn an NFT from the **connected wallet**.

```csharp
var data = await contract.ERC721.Burn("{{token_id}}");
```

<Details summary="Configuration">

#### tokenId

The token ID of the NFT to burn.

Must be a `string`.

</Details>
