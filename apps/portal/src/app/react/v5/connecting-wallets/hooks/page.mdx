import { ArticleIconCard, createMetadata, DocImage, Stack } from "@doc";
import { Wallet2Icon, ComponentIcon, Wallet } from "lucide-react";
import { ReactIcon } from "@/icons";

export const metadata = createMetadata({
	image: {
		title: "Conectar Carteras",
		icon: "react",
	},
	title: "Conectar Carteras en el SDK de React de thirdweb",
	description:
		"Conecta carteras usando ConnectButton, ConnectEmbed o el hook useConnect",
});

# Hooks de Conexión

Puedes crear una interfaz de usuario completamente personalizada para conectar la cartera usando el hook [`useConnect`](/references/typescript/v5/useConnect) para conectar cualquiera de las [carteras compatibles](/typescript/v5/supported-wallets).

```tsx
import { createThirdwebClient } from "thirdweb";
import { useConnect } from "thirdweb/react";
import { createWallet, injectedProvider } from "thirdweb/wallets";

const client = createThirdwebClient({ clientId });

function Example() {
	const { connect, isConnecting, error } = useConnect();
	return (
		<button
			onClick={() =>
				connect(async () => {
                    // create a wallet instance
					const metamask = createWallet("io.metamask"); // autocomplete the wallet id
                    // trigger the connection
					await metamask.connect({ client });
					// return the wallet
					return metamask;
				})
			}
		>
			Connect
		</button>
	);
}
```

## Crear Carteras

Puedes crear cualquier cartera por ID con autocompletado usando la función [`createWallet`](/references/typescript/v5/createWallet). O usa una de las carteras nativas como [`inAppWallet`](/references/typescript/v5/inAppWallet) o [`ecosystemWallet`](/references/typescript/v5/ecosystemWallet).

<Stack>
<ArticleIconCard
	title="inAppWallet"
	icon={Wallet}
	href="/references/typescript/v5/inAppWallet"
	description="Crear una cartera en la aplicación"
/>

<ArticleIconCard
	title="ecosystemWallet"
	icon={Wallet}
	href="/references/typescript/v5/ecosystemWallet"
	description="Crear una cartera de ecosistema"
/>

<ArticleIconCard
	title="createWallet"
	icon={Wallet}
	href="/references/typescript/v5/createWallet"
	description="Crear cualquier cartera externa por ID con autocompletado"
/>
</Stack>

## Hooks de Pre-conexión

Usa estos hooks para activar y gestionar la conexión de la cartera dentro de tu propia interfaz de usuario. Consulta la [referencia de hooks de conexión de cartera](/references/typescript/v5/useConnect) para más información.

<Stack>

<ArticleIconCard
	title="useConnect"
	icon={ReactIcon}
	href="/references/typescript/v5/useConnect"
	description="Hook para conectar cualquier cartera creada"
/>

<ArticleIconCard
	title="useAutoConnect"
	icon={ReactIcon}
	href="/references/typescript/v5/useAutoConnect"
	description="Hook para conectar automáticamente una cartera previamente conectada"
/>

<ArticleIconCard
	title="useConnectModal"
	icon={ReactIcon}
	href="/references/typescript/v5/useConnectModal"
	description="Hook para abrir un modal de conexión preconstruido desde cualquier lugar"
/>

<ArticleIconCard
	title="useSwitchActiveWalletChain"
	icon={ReactIcon}
	href="/references/typescript/v5/useSwitchActiveWalletChain"
	description="Hook para cambiar la cadena activa de la cartera"
/>

<ArticleIconCard
	title="useDisconnect"
	icon={ReactIcon}
	href="/references/typescript/v5/useDisconnect"
	description="Hook para desconectar la cartera"
/>

</Stack>

## Hooks Post-Conexión

Una vez que la cartera está conectada, puedes usar los [hooks de conexión de cartera](/references/typescript/v5/hooks#wallet-connection) para obtener información sobre la cartera conectada, como obtener la dirección, cuenta, etc.

<Stack>

<ArticleIconCard
	title="useActiveAccount"
	icon={ReactIcon}
	href="/references/typescript/v5/useActiveAccount"
	description="Obtener la cuenta actualmente conectada"
/>

<ArticleIconCard
	title="useActiveWallet"
	icon={ReactIcon}
	href="/references/typescript/v5/useActiveWallet"
	description="Obtener la cartera actualmente conectada"
/>

<ArticleIconCard
	title="useWalletDetailsModal"
	icon={ReactIcon}
	href="/references/typescript/v5/useWalletDetailsModal"
	description="Mostrar un modal preconstruido con detalles de la cartera desde cualquier lugar"
/>

<ArticleIconCard
	title="useConnectedWallets"
	icon={ReactIcon}
	href="/references/typescript/v5/useConnectedWallets"
	description="Obtener todas las carteras conectadas"
/>

<ArticleIconCard
	title="useAdminWallet"
	icon={ReactIcon}
	href="/references/typescript/v5/useConnectedWallets"
	description="Obtener la cartera administrativa subyacente de una cuenta inteligente"
/>

</Stack>
