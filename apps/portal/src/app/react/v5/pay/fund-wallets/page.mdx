import { ExternalLink } from "lucide-react";
import { ArticleIconCard, Stack } from "@doc";
import { ReactIcon } from "@/icons";
import { createMetadata } from "@doc";

export const metadata = createMetadata({
	title: "Buy Crypto | thirdweb",
	description: "Let users fund their wallets with crypto or fiat",
});

# Funding wallets

Let users fund their wallets with crypto or fiat on all the supported chains.

<Stack>

<ArticleIconCard
	title="Try the demo"
	icon={ExternalLink}
	href="https://playground.thirdweb.com/bridge/buy-widget"
	description="See the SDK in action on the live playground"
/>
</Stack>

## Usage with UI Components

The easiest way to buy crypto is to use the `BuyWidget` prebuilt UI component.

```tsx
import { BuyWidget } from "thirdweb/react";
import { base } from "thirdweb/chains";

function App() {
  return (
    <BuyWidget
      client={client}
      chain={base}
      amount="0.01"
      title="Get funds"
      // ... theme, onSuccess, onError, etc.
    />
  );
}
```

Check out the [BuyWidget API reference](/references/typescript/v5/BuyWidget) for more information.

<Stack>

<ArticleIconCard
	title="BuyWidget"
	icon={ReactIcon}
	href="/references/typescript/v5/BuyWidget"
	description="Prebuilt UI to buy crypto by swapping or fiat onramps"
/>

</Stack>


## Usage with your own UI

If you want more control over the funding flow, you can use the [Buy with Crypto or Fiat hooks](/references/typescript/v5/hooks#buy-crypto) to create your own custom UI.

<Stack>

<ArticleIconCard
	title="useBuyWithCryptoQuote"
	icon={ReactIcon}
	href="/references/typescript/v5/useBuyWithCryptoQuote"
	description="Get a quote for funding a wallet with crypto from another wallet, chain, or token"
/>

<ArticleIconCard
	title="useBuyWithFiatQuote"
	icon={ReactIcon}
	href="/references/typescript/v5/useBuyWithFiatQuote"
	description="Get a quote for funding a wallet with fiat"
/>
</Stack>