import { ExternalLink } from "lucide-react";
import { ArticleIconCard, Stack } from "@doc";
import { ReactIcon } from "@/icons";
import { createMetadata } from "@doc";

export const metadata = createMetadata({
	title: "Transacciones desde cualquier cadena o token | thirdweb",
	description: "Permite a los usuarios realizar transacciones desde cualquier cadena o token",
});

# Abstracción de Cadenas

Facilita las transacciones permitiendo que los usuarios paguen con cualquier token de cualquier cadena, o con fiat.

## Playground en Vivo

Prueba la demostración por ti mismo en el [playground en vivo](https://playground.thirdweb.com/connect/pay/transactions)

<Stack>

<ArticleIconCard
	title="Prueba la demo"
	icon={ExternalLink}
	href="https://playground.thirdweb.com/connect/pay/transactions"
	description="Mira el SDK en acción en el playground en vivo"
/>
</Stack>

## Uso con PayEmbed

Puedes usar el componente `PayEmbed` para permitir que los usuarios ejecuten una transacción desde cualquier cadena, pagando con cualquier token.

```tsx
import { claimTo } from "thirdweb/extensions/erc1155";
import { PayEmbed, useActiveAccount } from "thirdweb/react";

function App() {
  const account = useActiveAccount();
  const { data: nft } = useReadContract(getNFT, {
    contract: nftContract,
    tokenId: 0n,
  });

  return (
    <PayEmbed
      client={client}
      payOptions={{
        mode: "transaction",
        transaction: claimTo({
          contract: nftContract,
          quantity: 1n,
          tokenId: 0n,
          to: account?.address,
        }),
        metadata: nft?.metadata,
      }}
    />
  );
}
```

Consulta la [referencia de la API de PayEmbed](/references/typescript/v5/PayEmbed) para más información.

<Stack>

<ArticleIconCard
	title="PayEmbed"
	icon={ReactIcon}
	href="/references/typescript/v5/PayEmbed"
	description="Interfaz preconstruida para financiar billeteras con cripto o fiat"
/>

</Stack>

## Uso con transacciones regulares

Cualquier transacción enviada con `useSendTransaction` o con `TransactionButton` pedirá al usuario que elija un método de pago diferente si no tiene suficiente saldo para la transacción.

Puedes desactivar este comportamiento estableciendo la opción `payModal` en `false`.
