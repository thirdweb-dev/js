import Script from 'next/script';
import { Details, Callout, createMetadata } from "@doc";
import { Blueprint } from "../../../components/ui/blueprint";
import { createOAuthWalletNode } from "./nodes";

export const metadata = createMetadata({
    title: "Blueprint Overview | Thirdweb Unreal Engine SDK",
    description: "Overview of the Thirdweb Unreal Engine SDK Blueprint system.",
});

# Blueprint Overview

This section is currently under construction. Please check back soon for detailed information about the Thirdweb Unreal Engine SDK Blueprint system.

<Blueprint code={createOAuthWalletNode} />
<Script src="/lib/klee.min.js" />
