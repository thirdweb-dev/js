# Contrato

Un "contrato" es un contenedor alrededor de un contrato inteligente desplegado en una cadena. Es lo que usas para crear transacciones y leer el estado del contrato.

```ts
import { getContract } from "thirdweb";
import { ethereum } from "thirdweb/chains";

// get a contract
const contract = getContract({
  // the client you have created via `createThirdwebClient()`
  client,
  // the chain the contract is deployed on
  chain: ethereum,
  // the contract's address
  address: "0x123...",
  // OPTIONAL: the contract's abi
  abi: [...],
});
```

Los contratos definidos de esta manera son ligeros y pueden exportarse como constantes en toda tu aplicación para leer o escribir en ellos.
