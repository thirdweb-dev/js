import { GithubButtonLink } from "@doc";
import { createMetadata } from "@doc";

export const metadata = createMetadata({
  image: {
    title: "ERC1155Revealable contract",
    icon: "solidity",
  },
  title: "ERC1155Revealable | contrato de thirdweb",
  description:
    "Crea lotes de NFTs de 'revelación retardada' que pueden ser revelados en una fecha posterior después de haber sido acuñados y reclamados",
});

# ERC1155Revealable

```solidity
import "@thirdweb-dev/contracts/eip/ERC1155.sol";
import "@thirdweb-dev/contracts/extension/DelayedReveal.sol";
```

Crea lotes de NFTs de ['revelación retardada'](/glossary/delayed-reveal) que pueden ser revelados en una fecha posterior después de haber sido acuñados y reclamados.

Esta es una extensión que es detectable en el panel de control si el contrato inteligente implementa las extensiones
[`ERC1155`](/contracts/build/extensions/erc-1155/ERC1155) y [`DelayedReveal`](/contracts/build/extensions/general/DelayedReveal).

<GithubButtonLink href="https://github.com/thirdweb-dev/contracts/blob/main/contracts/extension/DelayedReveal.sol" />

## Uso

Este es un contrato inteligente de ejemplo que demuestra cómo heredar de esta extensión y sobrescribir las funciones para agregar funcionalidad personalizada (opcional).

```solidity
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

import "@thirdweb-dev/contracts/eip/ERC1155.sol";
import "@thirdweb-dev/contracts/extension/LazyMint.sol";
import "@thirdweb-dev/contracts/extension/DelayedReveal.sol";

contract Contract is ERC1155, LazyMint, DelayedReveal {
    constructor(
        string memory _name,
        string memory _symbol
    )
        ERC1155(
            _name,
            _symbol
        )
    {}

    function reveal(uint256 _index, bytes calldata _key)
        external
        override
        returns (string memory revealedURI)
    {
      // Your custom implementation here
    }

    function _canLazyMint() internal view override returns (bool) {
        // Your custom implementation here
    }
}
```

## Contratos de Base que implementan esta extensión

- [ERC1155DelayedReveal](/contracts/build/base-contracts/erc-1155/delayed-reveal)
- [ERC1155Drop](/contracts/build/base-contracts/erc-1155/drop)
