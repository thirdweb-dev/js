import { Callout } from "@doc";
import { GithubButtonLink } from "@doc";
import { createMetadata } from "@doc";

export const metadata = createMetadata({
	image: {
		title: "Contrato ERC721Supply",
		icon: "solidity",
	},
	title: "ERC721Supply | contrato de thirdweb",
	description:
		"Ver todos los NFTs de la colección implementando la función totalSupply en tu contrato ERC721(A)",
});

# ERC721Supply

```solidity
import "@thirdweb-dev/contracts/eip/interface/IERC721Supply.sol"
```

Ver todos los NFTs de la colección implementando la función `totalSupply` en tu contrato ERC721(A).

<Callout title="info" variant="info">

Esta extensión es una interfaz y requiere que todas las funciones sean implementadas

</Callout>

<br />

<GithubButtonLink href="https://github.com/thirdweb-dev/contracts/blob/main/contracts/eip/interface/IERC721Supply.sol" />

## Uso

Este es un ejemplo de contrato inteligente que demuestra cómo heredar de esta extensión y sobrescribir las funciones para agregar (opcionalmente) funcionalidades personalizadas.

El contrato [ERC721A](/contracts/build/extensions/erc-721/ERC721) viene con una función `totalSupply` que devuelve el número total de NFTs que han sido emitidos en la colección (incluyendo los tokens que han sido quemados).

Esto significa que ya puedes ver el suministro total de NFTs en la colección implementando el contrato `ERC721A`.

Opcionalmente, puedes sobrescribir la funcionalidad de esta función como se describe a continuación:

```solidity
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.0;

import "@thirdweb-dev/contracts/eip/ERC721A.sol";

contract Contract is ERC721A {
    constructor(
        string memory _name,
        string memory _symbol
    )
        ERC721A(
            _name,
            _symbol
        )
    {}

    function totalSupply() public view override returns (uint256) {
        // Your custom implementation here (overriding this function is optional)
    }

}
```

## Contratos de Base que implementan esta extensión

- [ERC721Base](/contracts/build/base-contracts/erc-721/base)
- [ERC721DelayedReveal](/contracts/build/base-contracts/erc-721/delayed-reveal)
- [ERC721Drop](/contracts/build/base-contracts/erc-721/drop)
- [ERC721LazyMint](/contracts/build/base-contracts/erc-721/lazy-mint)
- [ERC721SignatureMint](/contracts/build/base-contracts/erc-721/signature-mint)
