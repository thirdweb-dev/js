import { Container } from "../../../../components/basic";

export function EnterPassword(props: { onVerify: (password: string) => void }) {
  console.log(props.onVerify);
  return <Container>TODO</Container>;
}
