---
"@thirdweb-dev/wallets": patch
---

Fix RPCs not updated in given `chain` object when `clientId` is passed to the `EmbeddedWallet`, `PaperWallet`, `SmartWallet`, `BloctoWallet`, `SignerWallet` constructors.
